name: Push Trigger demo

on: 
  push: 
    branches: 
      - main
jobs:
  demo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show Message
        run: echo "Work flow started"

      - name: List Files
        run: ls -ltr

      - name: Read app.txt
        run: cat app.txt
